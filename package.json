{
  "name": "magic-house",
  "version": "1.0.0",
  "description": "Arcane Archive - TMDB/API proxy for tests",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test tests/smoke.test.js",
    "test:ui": "playwright test tests/ui-smoke.spec.js --workers=1"
  },
  "dependencies": {
    "@vercel/kv": "^1.0.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
