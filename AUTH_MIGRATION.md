# Authentication Migration Plan

## 목표

- 카카오 전용 로그인 흐름을 보조 수단으로 낮추고
- 이메일 + Google 중심의 글로벌 인증 구조로 전환한다.

## 왜 바꾸는가

- 카카오는 한국 사용자에게는 편하지만 글로벌 확장 기본값으로는 약하다.
- 기기 간 동기화와 결제 전환을 고려하면 이메일 계정이 기준점이 되는 편이 안정적이다.
- Google 로그인은 해외 사용자 접근성을 높인다.

## 권장 순서

1. 이메일 로그인(매직링크 또는 OTP) 추가
2. Google 로그인 추가
3. 익명 기록 → 계정 기록 병합 플로우 추가
4. 카카오는 한국 전용 보조 로그인으로 유지

## 구현 메모

- `next-app/src/app/auth/page.tsx`에서 준비 상태를 먼저 보여준다.
- 실제 인증 라이브러리는 이후 `NextAuth`, `Auth.js`, 또는 자체 세션 방식 중 하나로 결정한다.
- 카카오 사용자 ID만 믿는 현재 공유 구조는 장기적으로 교체 대상이다.
